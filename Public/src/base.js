(function() {
  // Core module.
  // Initialize the global REPLIT handler.
  var $, REPLIT;

  $ = jQuery;

  REPLIT = {};

  REPLIT.$this = $(REPLIT);

  // Export globally.
  this.REPLIT = REPLIT;

}).call(this);
